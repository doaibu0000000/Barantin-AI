# Workspace Rules

- **Git Auto-Push**: After completing any set of changes or modifications requested by the user, ALWAYS automatically commit and push the changes to the GitHub repository using `git add .`, `git commit -m "..."`, and `git push` without waiting for the user to ask.
